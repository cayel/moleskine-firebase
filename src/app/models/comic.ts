export class Comic {
    constructor(public title : string, public serie: string, public volume: number, public scenarist : string, public cartoonist : string, public date : number, public rating : number, public imageUrl: string) {
    }
}